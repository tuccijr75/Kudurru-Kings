> Migrated note: Legacy audit snapshot migrated from migrate.zip. Preserved as historical tracking, not live gameplay authority.

# Kudurru Kings — Reconciliation Audit Report v1.0

## Result

The shipping update pack is valid for planning, but not fully synchronized as doctrine.

## Reconciled in this patch
1. Multiplayer canon now consistently treats **2–4 players as first-class shipping support**, with more than 4 validation-gated.
2. Current build data is now explicitly recorded in doctrine as a **canonized implementation snapshot**.
3. The gameplay bridge/reference layer now points to the finalized doctrine hierarchy rather than older consolidation framing.
4. Engineering doctrine now records current build implementation realities that still differ from target canon.
5. The mechanics alignment matrix now includes the newly reconciled issues:
   - multiplayer doctrine mismatch
   - build snapshot specifics
   - stale source-precedence framing
   - build-vs-canon drift items

## Key current build facts recorded by this patch
From the current Base44 build snapshot:
- phase order is Upkeep → Draw → Edicts → Main → Battle → Capture → End
- players start with Sinew 2 / Sigil 1 / Oath 1
- Draw currently refills to hand limit 6 rather than canonical draw-1
- Edicts currently baseline to Name/Edict installs that cost Sigil and add Heat
- Battle currently uses simplified deterministic axis comparison
- Capture currently converts downed units into Marks directly in the implemented flow
- End currently grants +1 Sinew / +1 Sigil / +1 Oath
- the UI visibly supports a 4-seat multiplayer presentation

## Use
Apply this patch pack over the current project folder files with the same names.
